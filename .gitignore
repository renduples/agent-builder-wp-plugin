/node_modules/
/vendor/
.env.local
/.phpunit.result.cache
/.DS_Store
._*

# AI coding assistants / local tooling — never ship in the public plugin repo
/CLAUDE.md
/Claude.md
/.claude/
/.cursor/
/.windsurf/
/.continue/
.aider.chat.history.md
.aider.tags.cache.v4

# AppleDouble / SMB junk
._*
.DS_Store

# WP.org review notes — keep local/SVN-only, not the public GitHub tree
/SUBMISSION-NOTES.md

# Internal docs/design notes — local only, never published to the public repo
/docs/

# Prompt-test output — real LLM responses from your own site, real cost figures,
# rewritten on every run. A local artifact, like a coverage report.
/tests/prompt_test_results.md

# WP.org submission zips — build artifacts from bin/export-wporg-tree.sh, never commit
*.zip
/dist/
